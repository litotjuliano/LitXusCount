import MasterLayout from "../masterLayout/MasterLayout";
import ExpensesLayer from "../components/accounts/ExpensesLayer";

const ExpensesPage = () => (
  <MasterLayout>
    <ExpensesLayer />
  </MasterLayout>
);

export default ExpensesPage;
