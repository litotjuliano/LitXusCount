import MasterLayout from "../masterLayout/MasterLayout";
import GlAccountSettingsLayer from "../components/settings/GlAccountSettingsLayer";

const GlAccountsPage = () => (
  <MasterLayout>
    <GlAccountSettingsLayer />
  </MasterLayout>
);

export default GlAccountsPage;
