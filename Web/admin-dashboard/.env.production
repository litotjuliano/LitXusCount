# Empty = axios makes relative requests against the current origin.
# Works on App Platform because the api service is routed at /api on the
# same domain as this static site (see ../../.do/app.yaml).
VITE_API_BASE_URL=
